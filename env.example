# =============================================================================
# LOCAL DEVELOPMENT (.env file)
# =============================================================================
# Copy this file to .env and fill in your values for local development

# External API Keys (use test/development keys)
VAPI_API_KEY=vapi_your_key_here
OPENAI_API_KEY=sk-your_openai_key_here
TWILIO_ACCOUNT_SID=AC_your_sid_here
TWILIO_AUTH_TOKEN=your_auth_token_here
TWILIO_STATUS_CALLBACK_URL=http://localhost:3000/webhooks/twilio
# Twilio Voice URL (for inbound calls - connects to Vapi)
TWILIO_VOICE_URL=http://localhost:3000/voice

STRIPE_SECRET_KEY=sk_test_your_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_secret_here
STRIPE_SUCCESS_URL=http://localhost:3000/onboarding?session_id={CHECKOUT_SESSION_ID}
STRIPE_CANCEL_URL=http://localhost:3000/cancel

# Note: Stripe price IDs are managed programmatically via 'rails stripe:sync_products'
# No need to manually set STRIPE_PRICE_STARTER or STRIPE_PRICE_PRO

SENDGRID_API_KEY=SG.your_key_here
MAILER_FROM=noreply@localhost

# Sentry (replace with your actual DSN)
SENTRY_DSN=https://60fa33a0aac518ca509c8fa1cfa57692@o4510253094141952.ingest.us.sentry.io/4510253100433408

# Rails
RAILS_MASTER_KEY=your_master_key_here

# Application Host (for mailer URLs and webhook callbacks)
APP_HOST=localhost:3000
APP_URL=http://localhost:3000

# Background Jobs & Redis
# Sidekiq requires Redis for job queuing
REDIS_URL=redis://localhost:6379/1

# =============================================================================
# HEROKU PRODUCTION (set via: heroku config:set KEY=value)
# =============================================================================
# ⚠️ Phase 0 deployed to PRODUCTION with LIVE API keys
# Live app: https://beaker-ai-33941f73a135.herokuapp.com
#
# Do NOT use .env files on Heroku - set these via Heroku config vars
#
# Required for deployment:
#   heroku config:set RAILS_MASTER_KEY=$(cat config/master.key)
#   heroku config:set APP_HOST=beaker-ai-33941f73a135.herokuapp.com
#
# API Keys (⚠️ LIVE keys in production):
#   heroku config:set VAPI_API_KEY=<LIVE_key_from_vapi.ai>
#   heroku config:set OPENAI_API_KEY=<LIVE_key_from_openai.com>
#   heroku config:set TWILIO_ACCOUNT_SID=<LIVE_sid>
#   heroku config:set TWILIO_AUTH_TOKEN=<LIVE_token>
#   heroku config:set STRIPE_SECRET_KEY=sk_live_... # ⚠️ LIVE key
#   heroku config:set STRIPE_WEBHOOK_SECRET=whsec_... # ⚠️ LIVE secret
#   heroku config:set SENTRY_DSN=<your_sentry_dsn>
#
# Webhook URLs (production Heroku URLs):
#   heroku config:set TWILIO_STATUS_CALLBACK_URL=https://beaker-ai-33941f73a135.herokuapp.com/webhooks/twilio
#   heroku config:set TWILIO_VOICE_URL=https://beaker-ai-33941f73a135.herokuapp.com/voice
#   heroku config:set STRIPE_SUCCESS_URL=https://beaker-ai-33941f73a135.herokuapp.com/onboarding?session_id={CHECKOUT_SESSION_ID}
#   heroku config:set STRIPE_CANCEL_URL=https://beaker-ai-33941f73a135.herokuapp.com/cancel
#
# Optional:
#   heroku config:set SENDGRID_API_KEY=<if_using_sendgrid>
#   heroku config:set MAILER_FROM=noreply@beaker-ai-33941f73a135.herokuapp.com
#
# Database (automatically set by Heroku Postgres addon):
#   DATABASE_URL=postgres://... (auto-provisioned, do not set manually)
#
# Background Jobs:
#   Redis is required for Sidekiq (set automatically by heroku-redis addon)
#   Add Redis: heroku addons:create heroku-redis:mini -a beaker-ai
#   Worker dyno: heroku ps:scale worker=1 -a beaker-ai
# =============================================================================

